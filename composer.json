{
    "name": "level6/turnstile-bundle",
    "description": "Cloudflare Turnstile captcha for Symfony: widget rendering, canonical server-side siteverify and configurable request gating (login, password reset, any POST surface).",
    "type": "symfony-bundle",
    "license": "proprietary",
    "require": {
        "php": ">=7.1.3",
        "ext-json": "*",
        "symfony/config": "^4.0|^5.0|^6.0|^7.0|^8.0",
        "symfony/dependency-injection": "^4.0|^5.0|^6.0|^7.0|^8.0",
        "symfony/http-foundation": "^4.0|^5.0|^6.0|^7.0|^8.0",
        "symfony/http-kernel": "^4.0|^5.0|^6.0|^7.0|^8.0",
        "symfony/routing": "^4.0|^5.0|^6.0|^7.0|^8.0",
        "twig/twig": "^2.7|^3.0"
    },
    "suggest": {
        "ext-curl": "Used for the siteverify HTTP call when available; a stream-context fallback is used otherwise"
    },
    "autoload": {
        "psr-4": {
            "Level6\\TurnstileBundle\\": "src/"
        }
    },
    "extra": {
        "branch-alias": {
            "dev-2.x": "2.0-dev"
        }
    }
}